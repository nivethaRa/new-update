function report(e) {
	Ti.API.info('User selected: ' + e.value);
}

function saveClicked() {
	// Alloy.Globals.baseHomeView.add(Alloy.createController('/foodtruck/checkins/checkin_third').getView());
}
